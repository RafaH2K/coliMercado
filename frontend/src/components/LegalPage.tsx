// Placeholder honesto: NO es texto legal real. Se reemplaza por el
// documento definitivo una vez que lo revise un abogado.
export default function LegalPage({ title }: { title: string }) {
    return (
        <div>
            <h1>{title}</h1>
            <p className="muted">
                Esta página está en preparación. Aquí publicaremos el texto completo, revisado por un profesional
                legal, próximamente.
            </p>
        </div>
    );
}
